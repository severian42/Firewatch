import type { Evidence } from '../types';

// This file is kept for legacy purposes but should not be used.
// Evidence is now managed via the useEvidence hook and stored in localStorage.
export const mockEvidence: Evidence[] = [];